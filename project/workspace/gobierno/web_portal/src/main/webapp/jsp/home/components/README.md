# Here is where components that have been not yet been parametrized for reuse are placed.

The reason for this folder is apply sort of an ´Extract Method´ pattern to the HTML file that uses this components